import styled from "styled-components";

export const UserMenuTrigger = styled.div`
    height: fit-content;
    line-height: 1;

    padding: 0.5rem 0;

    display: flex;
    gap: 0.75rem;
    align-items: center;

    cursor: pointer;

	p {
		margin: 0;
	}
`
