# context-menu

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build context-menu` to build the library.

## Running unit tests

Run `nx test context-menu` to execute the unit tests via [Vitest](https://vitest.dev/).
