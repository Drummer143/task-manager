{
	"name": "zitadel-auth-ui",
	"$schema": "../../../node_modules/nx/schemas/project-schema.json",
	"sourceRoot": "apps/frontend/zitadel-auth-ui",
	"projectType": "application",
	"tags": [],
	"// targets": "to see all targets run: nx show project zitadel-auth-ui --web",
	"targets": {
		"dev": {
			"executor": "@nx/next:server",
			"options": {
				"dev": true,
				"port": 1472,
				"buildTarget": "zitadel-auth-ui:build"
			}
		}
	}
}
